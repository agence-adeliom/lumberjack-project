{% embed "blocks/block.html.twig" %}
    {% block content %}
        {% import "components/typography/typography.html.twig" as typography %}
        {{ typography.heading({
            content: fields.title,
            className: 'mb-6 lg:mb-10',
        }) }}
        {{ fn('do_shortcode', '[gravityform id="' ~ fields.form_id ~ '" title="false" description="false" ajax="true" tabindex="49" field_values="check=First Choice,Second Choice"]') }}
    {% endblock %}
{% endembed %}
